# FSM

running make in main folder will create exe file for bit flopping application.

To run email validator, just go to the folder and simply run make.

To run email validator, just do this:
```
./exe beneslami72@gmail.com
```
